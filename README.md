# Mario Jump (simple HTML game)

Open `index.html` in a browser to play. Controls:

- Space or click: jump
- R: restart after Game Over

To serve locally (recommended) run:

```bash
python -m http.server 8000
```

Then open http://localhost:8000 in your browser.
